# Claude instructions

Before finishing any task that changes the repository, run `scripts/check.sh` from the repository root. Do not report the task as complete unless the script passes. If a check cannot run or fails for reasons outside the task's scope, report that clearly in the final response.
